# Logo Image Required

Please save the Meme Mini-Radar logo (the radar image with meme faces) as:
- `logo.png` - Main logo (600x600px recommended)

The logo should be placed in this `/public` directory.

The logo is referenced in:
- `/app/layout.tsx` - for favicon and metadata
- `/components/Header.tsx` - for the header display
- `/baseapp.json` - for Base App marketplace
